auth users {
    table: users
}

table users {
    id: Id primary auto
    email: Email required unique
    password_hash: String(255) required
}

page "/admin" {
    requires auth
    permits "admin.view"

    html {
        <h1>Admin</h1>
        <p>Authenticated Zelyra page.</p>
    }
}
