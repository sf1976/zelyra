database main {
    engine: mariadb
}

table customers {
    id: Id primary auto
    name: String(100) required
}

page "/customer/{id}" {
    paginated 25

    load customer = sql<Customer> {
        SELECT id, name
        FROM customers
        WHERE id = :id
    }

    html { <h1>{customer.name}</h1> }
}
