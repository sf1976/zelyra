database main {
    engine: mariadb
}

table customers {
    id: Id primary auto
    name: String(100) required
}

page "/customers" {
    search {
        username
    }

    load customers = sql<Customer[]> {
        SELECT id, name
        FROM customers
    }

    html { <h1>Customers</h1> }
}
