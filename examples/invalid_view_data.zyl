database main {
    engine: mariadb
}

table customers {
    id: Id primary auto
    name: String(100) required
}

page "/customers/{name}" {
    load customer = sql<Customer> {
        SELECT username
        FROM customers
        WHERE name = :name
    }

    html {
        <h1>{customer.name}</h1>
    }
}
