# Zelyra 0.3.0 onboarding rehearsal — 2026-09-21

## English

This is an internal technical rehearsal, not the required novice-user study.
It must not be used to close the human onboarding checkbox.

- Source revision: `e017e18`
- Tested version: `zelyra 0.2.0`
- Host scope: local Linux x86_64 workspace
- Command: `ZELYRA_CLEAN_ONBOARDING_ROOT_PASSWORD=<local-test-value> bash tests/clean-onboarding-e2e.sh`
- Result: passed

The run installed the CLI into an isolated prefix and exercised fresh project
generation, repeat-safe setup, generated Compose/doctor validation, MariaDB
schema creation and bootstrap, CRUD HTTP operations, search/filter/sort/
pagination, invalid-query rejection, custom actions, CSRF-protected edit/delete,
German/English Learn/Work rendering, and cleanup.

The password came from the already-running local test container, was supplied
only through the process environment, and was not recorded here. The run gives
technical onboarding evidence; it does not measure whether a new human can
follow the documentation without assistance.

## Deutsch

Dies ist eine interne technische Probe und nicht die geforderte Studie mit
einer unerfahrenen Person. Der Einsteiger-Checkbox darf damit nicht geschlossen
werden.

- Quellrevision: `e017e18`
- Geprüfte Version: `zelyra 0.2.0`
- Umgebung: lokaler Linux-x86_64-Arbeitsbereich
- Befehl: `ZELYRA_CLEAN_ONBOARDING_ROOT_PASSWORD=<lokaler-testwert> bash tests/clean-onboarding-e2e.sh`
- Ergebnis: erfolgreich

Der Lauf installierte die CLI in ein isoliertes Präfix und prüfte frische
Projekterzeugung, wiederholbares Setup, generierte Compose-/Doctor-Prüfung,
MariaDB-Schemaerzeugung und Bootstrap, CRUD-HTTP-Abläufe,
Suche/Filter/Sortierung/Pagination, Ablehnung unbekannter Query-Felder,
benutzerdefinierte Aktionen, CSRF-geschütztes Bearbeiten/Löschen,
Deutsch/Englisch in Learn/Work sowie das Aufräumen.

Das Passwort stammte aus dem bereits laufenden lokalen Testcontainer, wurde nur
über die Prozessumgebung übergeben und hier nicht aufgezeichnet. Der Lauf
belegt den technischen Einstiegspfad, misst aber nicht, ob eine neue Person
der Dokumentation ohne Hilfe folgen kann.
