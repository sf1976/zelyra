# Zelyra 0.3.0 release-artifact rehearsal — 2026-09-21

## English

This is a local rehearsal against the current `0.2.0` workspace, not evidence
for a published `0.3.0` candidate.

- Source revision: `e017e18`
- Release metadata: `v0.2.0`, consistent with the workspace and CLI
- Command: `bash tests/release-artifact-smoke.sh`
- Result: passed on Linux x86_64
- Verified: archive packaging, SHA-256 sidecars, safe archive paths, one
  executable, packaged `zelyra --version`, and installer dry-run

The 0.3.0 candidate artifact and published update smoke gates remain open until
the workspace version, candidate tag, release workflow, and published assets
are all aligned.

## Deutsch

Dies ist eine lokale Probe gegen den aktuellen `0.2.0`-Arbeitsstand und kein
Nachweis für einen veröffentlichten `0.3.0`-Kandidaten.

- Quellrevision: `e017e18`
- Release-Metadaten: `v0.2.0`, konsistent mit Workspace und CLI
- Befehl: `bash tests/release-artifact-smoke.sh`
- Ergebnis: unter Linux x86_64 erfolgreich
- Geprüft: Archiv-Paketierung, SHA-256-Sidecars, sichere Archivpfade, genau
  eine ausführbare Datei, `zelyra --version` aus dem Paket und Installer-
  Dry-Run

Die Gates für 0.3.0-Kandidatenartefakte und den veröffentlichten Update-Smoke-
Test bleiben offen, bis Workspace-Version, Kandidaten-Tag, Release-Workflow und
veröffentlichte Artefakte übereinstimmen.
